pub fn sort() {
    get_random_strings();
    
}

fn get_random_strings() {// -> Vec<&'static str> {
    let strings = vec!["Mick", "Craig", "Wayne", "Simon"];
    dbg!(strings);
    // return strings;
}

